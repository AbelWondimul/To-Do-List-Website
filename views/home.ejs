<%- include("header") -%>
    <div class="box" id = "heading">
   <h1>List of your To-do list</h1> 
   
    </div>
    
    <div class="box">
        <br>
            <% list.forEach((item) => {%>
                <div class="box1" >
                    <form action="/deleteList" method="post">                
                        <div class="item" >
                            <input type = "checkbox" onChange="this.form.submit()" name="checkBox" value="<%= item._id %>">
                            <p style="color:aliceblue"><%= item.name %></p>
                        </div>
                        <input type="hidden" name="listName" value="<%= item.name %>"> </input>  
                    </form>
                    <form  class="b2" action="/<%= item.name %>"  method="get">
                        <button type="submit" class="button b2" >→</button>
                    </form>
                </div>
                
                
            <% }) %>
           
        <form class ="item" action="/Home" method="post">
        <input type= "text" name="newItem" placeholder="Add new To-do List" autocomplete="off">
        <button type="submit" class="button" name="button" value="<%= list.name %>">+</button>
        </form>
        

        <br>
    </div>
<%- include("footer") -%>